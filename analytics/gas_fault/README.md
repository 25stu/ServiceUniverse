# Gas Fault analytics

Owner: Member B.

Expected subdirectories:

- `simulation/`: deterministic event-log generator;
- `event_logs/`: raw and explicitly labelled simulated logs;
- `mining/`: repeatable PM4Py analysis;
- `results/`: generated figures and tables, ignored except intentional examples.

Every activity value must match the frozen BPMN activity dictionary.
